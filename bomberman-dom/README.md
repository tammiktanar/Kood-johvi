# bomerman-dom

## Starting
1. Open your terminal in this project's directory
2. Run `npm install`
3. Run `npm run server`
4. Open another terminal in this project's directory
5. Run `npm run dev`
6. Open your browser at http://localhost:8080/